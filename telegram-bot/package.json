{
  "name": "benstream-telegram-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Bot Telegram VOD — sert films/séries depuis un canal privé (hébergé sur Vercel)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit",
    "setup-webhook": "node --import tsx scripts/setup-webhook.ts"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vercel/node": "^5.0.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
